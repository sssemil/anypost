# Start the relay first: pnpm --filter anypost-relay build && pnpm --filter anypost-relay start
# Copy the /ws/ multiaddr from the relay output below:
VITE_RELAY_MULTIADDR=/ip4/127.0.0.1/tcp/9090/ws/p2p/<RELAY_PEER_ID>
